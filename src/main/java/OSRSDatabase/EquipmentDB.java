package OSRSDatabase;

public class EquipmentDB extends OSRSDataBase
{
    //TODO have progression of armor, with a check if its buyable, untradable (perhaps some script attached to retrieve it)
    // presets based on activity, check sub accountbuilder for presets
}
