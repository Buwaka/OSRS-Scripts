package IF.Logic.Cycles.General;

import IF.Utilities.Scripting.SimpleCycle;

public class TimeManagementCycle extends SimpleCycle
{
    public TimeManagementCycle(String name, String authKey)
    {
        super(name, authKey);
    }
    // Basically has wait conditions on certain tasks
    // does miscellaneous tasks or waits in the meantime
}
